#!/bin/bash
# hydrocodone's setwallpaper
# generates a colorscheme of a random background and sets the wallpaper using feh

bgdir=/home/hydrocodone/Images/backgrounds
wal -i $bgdir -n
feh --no-fehbg --bg-fill --geometry +100 "$(</home/hydrocodone/.cache/wal/wal)"
